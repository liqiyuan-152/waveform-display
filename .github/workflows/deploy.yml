name: Deploy demo

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  tag-gate:
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name }}
    outputs:
      deploy: ${{ steps.gate.outputs.deploy }}
    steps:
      - id: gate
        shell: bash
        run: |
          if [[ "${RELEASE_TAG}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "deploy=true" >> "${GITHUB_OUTPUT}"
          else
            echo "deploy=false" >> "${GITHUB_OUTPUT}"
          fi

  build:
    needs: tag-gate
    if: needs.tag-gate.outputs.deploy == 'true'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build:demo
      - uses: actions/upload-artifact@v4
        with:
          name: waveform-display-site
          path: dist-demo
          if-no-files-found: error

  deploy:
    needs: [tag-gate, build]
    if: needs.tag-gate.outputs.deploy == 'true'
    runs-on: ubuntu-latest
    environment: production
    env:
      RELEASE_TAG: ${{ github.event.release.tag_name }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: waveform-display-site
          path: site
      - name: Configure deployment SSH
        shell: bash
        env:
          DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          DEPLOY_KNOWN_HOSTS: ${{ secrets.DEPLOY_KNOWN_HOSTS }}
        run: |
          test -n "$DEPLOY_SSH_PRIVATE_KEY"
          test -n "$DEPLOY_KNOWN_HOSTS"
          install -d -m 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          printf '%s\n' "$DEPLOY_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/id_ed25519 ~/.ssh/known_hosts
      - name: Upload and activate release
        shell: bash
        run: |
          ssh -o StrictHostKeyChecking=yes site-deploy@lqycustomsite.online "/usr/local/bin/deploy-static prepare waveform-display ${RELEASE_TAG}"
          rsync -azc --delete --itemize-changes --chmod=Du=rwx,Dgo=rx,Fu=rw,Fgo=r -e 'ssh -o StrictHostKeyChecking=yes' site/ site-deploy@lqycustomsite.online:/srv/project-deployments/waveform-display/.staging/${RELEASE_TAG}/
          ssh -o StrictHostKeyChecking=yes site-deploy@lqycustomsite.online "/usr/local/bin/deploy-static publish waveform-display ${RELEASE_TAG} ${GITHUB_SHA}"
